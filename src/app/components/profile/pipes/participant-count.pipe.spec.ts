import { ParticipantCountPipe } from './participant-count.pipe';

describe('ParticipantCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipantCountPipe();
    expect(pipe).toBeTruthy();
  });
});
