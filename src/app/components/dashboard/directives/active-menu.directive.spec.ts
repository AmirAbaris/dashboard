import { ActiveMenuDirective } from './active-menu.directive';

describe('ActiveMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveMenuDirective();
    expect(directive).toBeTruthy();
  });
});
