import { ActivateMenuChildDirective } from './activate-menu-child.directive';

describe('ActivateChildDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivateMenuChildDirective();
    expect(directive).toBeTruthy();
  });
});
